# 구현 메모

## 핵심 원칙

- Word 원문은 수정하지 않습니다.
- 괄호 안 도면부호는 조사 판단 시에만 제외합니다.
- 결과에는 원문 표현을 그대로 표시합니다.

## 현재 검출 가능 예시

- `뉴로모픽 시스템(100)는` → `뉴로모픽 시스템(100)은`
- `시냅스 회로(120)을` → `시냅스 회로(120)를`
- `출력단(130)가` → `출력단(130)이`
- `잇다` → `있다`

## 현재 한계

- 조사 검사는 받침 기반 규칙 검사이므로 문맥상 자연스러움까지 완벽히 판단하지 않습니다.
- `.doc` 파일은 지원하지 않고 `.docx`만 지원합니다.
- 자동 수정은 하지 않고 오류 후보와 수정 제안만 표시합니다.
